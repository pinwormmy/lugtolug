---
import BaseLayout from "@/layouts/BaseLayout.astro";
import WatchesListSearch from "@/components/WatchesListSearch";
import { getDb, listRecentSearchWatches } from "@/lib/db";
import { readSearchState } from "@/lib/searchState";

const initialSearchState = readSearchState(Astro.url.searchParams);
const initialWatches = await listRecentSearchWatches(getDb(Astro.locals), 48);
---

<BaseLayout title="Watch database: every lug-to-lug measurement | Lug to Lug Finder" description="Browse every watch in the database by brand, model, and reference, each with lug-to-lug, case diameter, thickness, and lug width.">
  <WatchesListSearch client:load initialQuery={initialSearchState.query} initialWatches={initialWatches} />
</BaseLayout>
